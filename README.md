# IPO Intelligence v0.3
First working IPO Ji HTML ingestion MVP.

Run:
`pip install -r requirements.txt`
`streamlit run app.py`

The Refresh button fetches IPO Ji's current IPO page and stores parsed IPO cards in SQLite.
Next: verify live parsing, add individual IPO detail/subscription/GMP snapshots, then scheduled polling.
